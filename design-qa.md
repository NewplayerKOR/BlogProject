# 포트폴리오 전면 개편 Design QA

## 비교 기준

- 기준 시안: `C:\Users\parkt\.codex\generated_images\019facf3-cafc-7151-800b-0dec46c6299a\exec-28a2b83e-53c5-4c54-a302-e90dafc7998a.png`
- 최종 구현 캡처: `design-qa-assets/implementation-home-1512x1067-production.png`
- 나란히 비교한 증거: `design-qa-assets/comparison-home-1512x1067-production.png`
- 브라우저 상태: 홈, 라이트 테마, 초기 화면
- 요청한 CSS 뷰포트: 1512×1067, device scale factor 1
- 실제 캡처 래스터: 기준 1486×1059, 구현 1497×1056

두 화면 모두 첫 화면의 헤더, 좌측 성장 서사, 우측 Project Growth Map, 하단 3단계 요약이 충분히 읽히므로 별도의 확대 크롭 없이 전체 화면 비교만으로 핵심 영역을 판정했다.

## 최종 비교

### 레이아웃과 간격

- 기준 시안의 좌우 2열 구성, 얇은 헤더 구분선, 하단 3분할 성장 요약을 동일한 정보 구조로 구현했다.
- 제목과 본문, CTA, Growth Map 사이의 시각적 위계가 유지되고 겹침·잘림·과도한 카드화는 없다.
- 기준의 점선 곡선 연결은 구현에서 얇은 직선 연결로 단순화됐지만, 성장 단계의 순서와 시각적 흐름은 유지된다. 기능과 위계를 해치지 않는 P3 수준 차이로 판정했다.

### 폰트와 색상

- 본문·제목은 `Pretendard Variable`, 코드·보조 라벨은 `Geist Mono`를 사용한다.
- 짙은 네이비 텍스트와 청록색 강조색을 semantic token으로 통일했다.
- 제목이 2줄 위계를 유지하며 잘리거나 컨테이너 밖으로 넘치지 않는다.

### 아이콘과 표면

- 성장 단계와 GitHub 등 모든 노출 아이콘은 Lucide 또는 브랜드 아이콘 라이브러리를 사용했다.
- 임의 SVG, 이모지, CSS 그림, 가짜 이미지 자산은 사용하지 않았다.
- 얇은 테두리와 최소한의 배경 처리로 기준 시안의 편집형 포트폴리오 분위기를 유지했다.

### 반응형과 접근성

- 375×812 모바일 프로젝트 화면: `design-qa-assets/implementation-projects-375x812-pass2.png`
- 375px에서 모바일 메뉴 Sheet를 열고 프로젝트 링크로 이동하는 흐름을 확인했다.
- 768px에서 문서 폭과 제목 영역이 뷰포트 안에 들어오며 가로 스크롤이 발생하지 않았다.
- 1280px에서 게시글 우측 목차가 표시되고 모바일 목차는 숨겨지는 것을 확인했다.
- 본문 바로가기, 의미 있는 `aria-label`, 키보드 포커스 스타일, 모바일 터치 영역, `prefers-reduced-motion` 대응을 포함했다.

### 게시글 기능

- Markdown을 HTML로 변환한 뒤 sanitize, heading slug, 자동 앵커 링크 순서로 처리한다.
- 모바일 목차 펼치기, 제목 앵커, 코드 복사 버튼 5개, 이전·다음 글 탐색 구조를 확인했다.
- 코드 복사 후 버튼 문구가 `복사됨`으로 변경되는 것을 확인했다.
- 브라우저 `error`/`warn` 로그는 없었다.

## 수정 이력

1. 초기 화면에서 제목과 Growth Map 비율이 기준과 달라 2열 너비, 타이포그래피 크기, 내부 간격을 조정했다.
2. shadcn Separator의 방향 데이터 속성이 런타임과 맞지 않아 구분선이 0px이던 문제를 방향 기반 클래스로 수정했다.
3. 3.5rem 제목에서 오른쪽 글자가 잘리던 문제를 3.35rem과 고정 줄 구조로 조정했다.
4. 개발 도구 오버레이가 없는 프로덕션 서버에서 최종 비교 캡처를 다시 생성했다.

## 정적 검증

- `npm.cmd run lint`: 통과
- `npx.cmd tsc --noEmit --incremental false`: 통과
- `npm.cmd run build`: 통과
- `git diff --check`: 오류 없음

`baseline-browser-mapping` 데이터 갱신 안내와 네트워크가 제한된 빌드 환경에서 OG 이미지의 동적 한글 폰트 로드 안내는 있었지만, 정적 페이지 16개와 OG 이미지 라우트 생성은 완료됐다.

최종 결과: passed
